<UserControl x:Class="AulasSiencb2.Views.LoadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AulasSiencb2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.RenderTransform>
        <TranslateTransform x:Name="Transform"/>
    </UserControl.RenderTransform>
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Cargando..." FontSize="24" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center"/>
            <ProgressBar Foreground="{DynamicResource PrimaryBrush}" BorderBrush="{DynamicResource HoverBrush}" IsIndeterminate="True" Width="200" Height="20" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
